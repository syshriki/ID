package ID::PatchParser::ABB;
use strict;
use warnings;
use base 'ID::PatchParser';

#my $unparsed = shift;
#my $object = new ID::PatchParser($unparsed);

sub Parse{
	my $self = shift;
        return "ABBPARSE";
}

1;
	
